# Compliance RAG — runtime deps only (what `streamlit run app.py` imports).
# Python 3.11, pinned in .python-version. Dev/eval extras: requirements-dev.txt.

# torch ships CUDA wheels from PyPI (~2.5GB unpacked), which does not fit in a
# Streamlit Community Cloud container. This index serves the CPU-only build.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- vector store + retrieval ---
chromadb==0.5.23
langchain-chroma==0.2.3
# chromadb needs sqlite >= 3.35; _bootstrap.py swaps this in only when the host
# sqlite3 is older. Harmless and unused on an up-to-date image.
pysqlite3-binary==0.5.4; sys_platform == "linux"

# --- embeddings (vectorstore.py) ---
sentence-transformers==3.3.1
transformers==4.46.3
# 2.6.0 is the newest cp311 CPU wheel on the index above. Local dev runs
# 2.13.0 (an MPS build for Apple Silicon); there is no Linux cp311 CPU
# equivalent, so the deployed app is a version behind on purpose.
torch==2.6.0

# --- LLM endpoint + prompt plumbing (chat.py, rag.py) ---
langchain-core==0.3.74
langchain-huggingface==0.3.1
huggingface_hub==0.36.2

# --- ingest only, but pure-python and tiny ---
langchain-text-splitters==0.3.9

# --- UI ---
streamlit==1.48.0

# --- misc ---
python-dotenv==1.0.1
numpy==1.26.4
